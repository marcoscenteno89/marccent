# from ninja_jwt.backends import JWTBackend

# class MyJWTBackend(JWTBackend):
#   def authenticate(self, request, token):
#     payload = self.decode_token(token)
#     # Here you can add additional checks based on the payload
#     return payload